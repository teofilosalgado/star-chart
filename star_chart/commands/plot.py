import logging

logger = logging.getLogger(__name__)


def plot():
    """Plot astronomical data into high-resolution PDF files using a QGIS project as its
    layout, styling and symbology source."""
    logger.info("Hello, world!")
